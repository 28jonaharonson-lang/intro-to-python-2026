import os, time, random

def textDelay1(text, delay=0.001):
    for char in text:
        print(char, end='', flush=True)
        time.sleep(delay)
    print() 


def textDelay2(text, delay=1):
    for char in text:
        print(char, end='', flush=True)
        time.sleep(delay)
    print() 



def enter():
    global wallet
    os.system('cls' if os.name == 'nt' else 'clear')
    print ("wallet: {}".format(wallet) + "$")
    textDelay1("You enter a casino, there are 3 different games: A slot machine, a blackjack table, and a roulette wheel. There is also a shop in the casino you can go to. Type which one you want to go to. ")
    choice = input().strip().lower()
    if choice == ("slot machine"):
        slot()
    elif choice == ("blackjack table"):
        blackjack()
    elif choice == ("roulette wheel"):
        roulette()
    elif choice == ("shop"):
        shop()
    else:
        textDelay1("Sorry, that isn't a location.")
        time.sleep(2)
        enter()


def slot():
    global wallet
    os.system('cls' if os.name == 'nt' else 'clear')
    print ("wallet: {}".format(wallet) + "$")
    textDelay1("You go to the slot machine, and there are two different prizes: the minor prize where two numbers are the same, and the jackpot where all three numbers are the same. The minor prize pays out 3x, and the jackpot pays out 75x. Type how much moneyyou want to spend.")
    choice = input().strip().lower()
    slot1 = random.randint(1, 5)
    slot2 = random.randint(1, 5)
    slot3 = random.randint(1, 5)  
    textDelay2(str(slot1) + str(slot2) + str(slot3))
    if slot1 == slot2 and slot2 == slot3:
        wallet = wallet + int(choice)*75
        textDelay1("You won the jackpot! You won {}".format(int(choice)*75) + "$")
        time.sleep(3)
        slot()
    elif slot1 == slot2 or slot2 == slot3 or slot1 == slot3:
        wallet = wallet + int(choice)*3
        textDelay1("You won the minor prize! You won {}".format(int(choice)*3) + "$")
        time.sleep(3)
        slot()
    else:
        wallet = wallet - int(choice)
        textDelay1("You won nothing. You lost {}".format(choice) + "$")
        time.sleep(2)
        slot()
    

    
    if int(choice) > int(wallet):
        textDelay1("You do not have that much money.")
    
def blackjack():
    os.system('cls' if os.name == 'nt' else 'clear')

def roulette():
    os.system('cls' if os.name == 'nt' else 'clear')

def shop():
    os.system('cls' if os.name == 'nt' else 'clear')














wallet = 100

enter()